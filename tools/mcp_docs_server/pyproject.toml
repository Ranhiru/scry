[project]
name = "mcp-docs-server"
version = "0.1.0"
description = "MCP server for workspace documentation search"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pyyaml>=6.0",
    "snowballstemmer>=3.0",
    "zvec>=0.3.1,<0.4",
]
